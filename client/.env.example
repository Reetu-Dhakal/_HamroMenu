# ================ HamroMenu client (Vite + React) ================
# API base. Leave empty in dev — Vite proxies /api -> localhost:5000
VITE_API_URL=
# Socket server. Leave empty in dev — proxied through Vite.
VITE_SOCKET_URL=
# Khalti public key for the checkout widget (sandbox key looks like test_...)
VITE_KHALTI_PUBLIC_KEY=